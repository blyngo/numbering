name: flutter_application_4
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.8.0
  logger: ^2.5.0
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.5
  permission_handler: ^11.4.0
  url_launcher: ^6.1.5
  sqflite: ^2.4.1
  flutter_localizations:
    sdk: flutter
  share_plus: ^10.1.4
  flutter_file_dialog: ^3.0.2 #flutter_file_dialog 추가
  encrypt: ^5.0.1
  path_provider_android: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.3

flutter:
  uses-material-design: true

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/app_icon.PNG"
